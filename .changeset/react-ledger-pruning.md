---
"@sigilry/react": patch
---

Handle pruned ledger update offsets and oversized update windows in `useLedgerUpdates` and `useContractStream`, with shared request-shaping helpers for Canton JSON API consumers.
