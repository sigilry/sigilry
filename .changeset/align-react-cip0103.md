---
"@sigilry/react": patch
---

Align with CIP-0103 amendment: lowercase HTTP methods, object-form request bodies, PascalCase command atoms in `useExerciseChoice`, and split `connect()`/`status()` reads in the React context.
