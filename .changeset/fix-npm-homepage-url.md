---
"@sigilry/dapp": patch
"@sigilry/react": patch
"@sigilry/cli": patch
"@sigilry/canton-json-api": patch
"@sigilry/splice-dars": patch
---

Set npm `homepage` to https://sigilry.org. Previously the published packages pointed at `sigilry.github.io` (or, for `@sigilry/splice-dars`, the GitHub README) — re-publish so the npm package pages link to the canonical site. `@sigilry/splice-dars` also gains a `bugs` field for parity with the other packages.
